<div>
  <h1 class="font-bold text-4xl">Product#index</h1>
  <p>Find me in app/views/product/index.html.erb</p>
</div>

<div class="mt-4">
  <% @products.each do |product| %>
    <div>
      <h2 class="text-green-500 font-bold"><%= product.name %></h2>
      <%= link_to 'Show Product', product_path(product) %>
    </div>
  <% end %>
</div>
